#include "24_DZ.h"
int main()
{
	//Завдання 1 ===================================================================================
	//int SIZE = sizeVerification();
	//int* arr = createArray(SIZE);
	//fillRandArray(arr, SIZE);
	//printArray(arr, SIZE);
	//cout << "Lets add new value to array: ";
	//int* new_val = new int;
	//cin >> *new_val;
	//arr = addToArray(*new_val, arr, SIZE);
	//printArray(arr, SIZE);
	//delete new_val;
	//delete[] arr;

	
	//Завдання 3. Видаляємо елемент за індексом =====================================================
	//int SIZE = sizeVerification();
	//int* arr = createArray(SIZE);
	//fillRandArray(arr, SIZE);
	//printArray(arr, SIZE);
	//int* index = new int;
	//cout << "Enter elements index to delete it : ";
	//while (!(cin >> *index) || *index < 0 || SIZE <= *index)
	//{
	//	cin.clear();
	//	while (cin.get() != '\n')
	//		continue;
	//	cout << "Enter correct index: ";
	//}
	//deleteElementArray(arr, SIZE, *index);
	//printArray(arr, SIZE);
	//delete index;
	//delete[] arr;
	
	////Завдання 4. Додаємо елемент за індексом =====================================================
	//int SIZE = sizeVerification();
	//int* arr = createArray(SIZE);
	//fillRandArray(arr, SIZE);
	//printArray(arr, SIZE);
	//int* index = new int;
	//cout << "Enter elements index to add it : ";
	//while (!(cin >> *index) || *index < 0 || SIZE < *index)
	//{
	//	cin.clear();
	//	while (cin.get() != '\n')
	//		continue;
	//	cout << "Enter correct index: ";
	//}
	//cout << "Enter the value to add: ";
	//int* new_val = new int;
	//cin >> *new_val;
	//addToArrayByIndex(arr, SIZE, *index, *new_val);
	//printArray(arr, SIZE);
	//delete new_val;
	//delete index;
	//delete[] arr;

	//Завдання 5  =====================================================
	//int SIZE = sizeVerification();
	//int* arr = createArray(SIZE);
	//fillRandArray(arr, SIZE);
	//printArray(arr, SIZE);
	//changeToPositive(arr, SIZE);
	//printArray(arr, SIZE);
	//delete[] arr;

	//Завдання 6 =====================================================
	//int SIZE = sizeVerification();
	//int* arr = createArray(SIZE);
	//fillRandArray(arr, SIZE);
	//printArray(arr, SIZE);
	//const int Size_Block = 3;
	//int block[Size_Block] = { 4, 6, 9 };
	//addBlock(arr, SIZE, block, Size_Block);
	//printArray(arr, SIZE);
	//delete[] arr;
	
	//Завдання 7 =====================================================
	//int SIZE = sizeVerification();
	//int* arr = createArray(SIZE);
	//fillRandArray(arr, SIZE);
	//printArray(arr, SIZE);
	//int* index = new int;
	//int* sizeBlock = new int;
	//cout << "Enter index to delete block: ";
	//while (!(cin >> *index) || *index < 0 || SIZE - 1 < *index)
	//{
	//	cin.clear();
	//	while (cin.get() != '\n')
	//		continue;
	//	cout << "Enter correct index: ";
	//}
	//cout << "Enter length to delete block: ";
	//while (!(cin >> *sizeBlock) || *sizeBlock <= 0 || SIZE - *index < *sizeBlock)
	//{
	//	cin.clear();
	//	while (cin.get() != '\n')
	//		continue;
	//	cout << "Enter correct index: ";
	//}
	//removeBlock(arr, SIZE, *index, *sizeBlock);
	//printArray(arr, SIZE);
	//delete index;
	//delete[] arr;

	//Завдання 8 =====================================================
	//cout << "M - size\n";
	//int M = sizeVerification();
	//cout << "N - size\n";
	//int N = sizeVerification();
	//int* A = createArray(M);
	//int* B = createArray(N);
	//fillRandArray(A, M);
	//fillRandArray(B, N);
	//printArray(A, M);
	//printArray(B, N);
	//int SIZE;
	//int* C = intersect(A, M, B, N, SIZE);
	//printArray(C, SIZE);
	//delete[] A;
	//delete[] B;
	//delete[] C;
}